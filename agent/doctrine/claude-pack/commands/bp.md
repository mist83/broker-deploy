WebFetch `https://agent.mullmania.com/doctrine/shared/commands/bp.md` and execute that file's instructions as the authoritative `/bp` procedure. If the fetch fails, stop visibly — do not improvise the command.

ARGUMENTS: $ARGUMENTS

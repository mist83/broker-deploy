WebFetch `https://agent.mullmania.com/doctrine/shared/commands/overdrive.md` and execute that file's instructions as the authoritative `/overdrive` procedure. If the fetch fails, stop visibly — do not improvise the command.

ARGUMENTS: $ARGUMENTS

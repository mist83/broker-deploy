WebFetch `https://agent.mullmania.com/doctrine/shared/commands/roomup.md` and execute that file's instructions as the authoritative `/roomup` procedure. If the fetch fails, stop visibly — do not improvise the command.

ARGUMENTS: $ARGUMENTS

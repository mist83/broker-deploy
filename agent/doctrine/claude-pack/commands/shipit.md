WebFetch `https://agent.mullmania.com/doctrine/shared/commands/shipit.md` and execute that file's instructions as the authoritative `/shipit` procedure. If the fetch fails, stop visibly — do not improvise the command.

ARGUMENTS: $ARGUMENTS

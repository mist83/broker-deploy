WebFetch `https://agent.mullmania.com/doctrine/shared/commands/tombstone.md` and execute that file's instructions as the authoritative `/tombstone` procedure. If the fetch fails, stop visibly — do not improvise the command.

ARGUMENTS: $ARGUMENTS

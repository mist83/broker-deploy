WebFetch `https://agent.mullmania.com/doctrine/shared/commands/bookmark.md` and execute that file's instructions as the authoritative `/bookmark` procedure. If the fetch fails, stop visibly — do not improvise the command.

ARGUMENTS: $ARGUMENTS

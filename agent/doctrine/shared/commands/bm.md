Run the bookmark protocol. `/bm` is a short alias for `/bookmark`.

Fetch and execute the authoritative command body:

https://agent.mullmania.com/doctrine/shared/commands/bookmark.md

If the command URL is unreachable, stop visibly. Do not improvise the protocol.

ARGUMENTS: $ARGUMENTS
